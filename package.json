{
    "name": "nw.js-example",
    "version": "1.0.0",
    "devDependencies": {
        "nw-builder": "^3.1.2"
    },
    "scripts": {
        "debug": "nw ./src/ --remote-debugging-port=8080",
        "build": "nwbuild ./src/ --mode=build --platforms win32,win64,osx64,linux32,linux64 --outDir build/"
    },
    "dependencies": {
        "nw": "^0.74.0"
    }
}
